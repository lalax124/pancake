# Local development only
GROQ_API_KEY=your_groq_api_key
# Optional: set this only if your API is deployed somewhere else.
# On Vercel, leave VITE_API_URL empty so the React app uses the same domain.
VITE_API_URL=
